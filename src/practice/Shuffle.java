package practice;

public class Shuffle {
    public static int main(String[] args) {
        int count;
        int[] array = {4, 9, 4, 4, 7, 9, 1, 7};
        for (count = 0; count < array.length; count++) {
            if (array.length == 0) {
                return array.length;
            } else {
                count++;
            }
            return count;
        }
        return count;

    }

    public static char array(char[] chars){
        char[] x = new char['n'];
        List<String> myList = new ArrayList<String>();
        myList.add("A");
        myList.add("M");
        myList.add("C");
        myList.add("W");
        myList.add("I");
        myList.add("T");

        for (char count = 0; count < myList.) {

            System.out.println(myList);

        }
    }